# Domain Layer

Pure Dart code with no Flutter imports. Contains:

- **entities/** — Core business objects (Album, Artist, Track, etc.)
- **repositories/** — Abstract interfaces for data access

This layer is independent of the UI and data layers.
