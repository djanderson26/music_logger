import 'package:flutter/material.dart';

/// ArtistDetail screen — placeholder implementation.
/// TODO: Implement in the relevant milestone.
class ArtistDetailScreen extends StatelessWidget {
  const ArtistDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ArtistDetail')),
      body: const Center(child: Text('ArtistDetail — coming soon')),
    );
  }
}
