import 'package:flutter/material.dart';

/// AlbumDetail screen — placeholder implementation.
/// TODO: Implement in the relevant milestone.
class AlbumDetailScreen extends StatelessWidget {
  const AlbumDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AlbumDetail')),
      body: const Center(child: Text('AlbumDetail — coming soon')),
    );
  }
}
